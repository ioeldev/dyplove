export const NAV_THEME = {
	light: {
		background: 'hsl(223 50% 98%)', // background from --background
		border: 'hsl(240 6% 92%)', // border from --border
		card: 'hsl(223 50% 98%)', // card from --card
		notification: 'hsl(0 84% 60%)', // destructive from --destructive
		primary: 'hsl(240 10% 4%)', // primary from --primary
		text: 'hsl(240 10% 4%)', // foreground from --foreground
	},
	dark: {
		background: 'hsl(220 100% 8%)', // background from --background
		border: 'hsl(0 0% 100% / 0.1)', // border from --border
		card: 'hsl(220 100% 8%)', // card from --card
		notification: 'hsl(0 72% 51%)', // destructive from --destructive
		primary: 'hsl(0 0% 98%)', // primary from --primary
		text: 'hsl(248 0% 98%)', // foreground from --foreground
	},
};
